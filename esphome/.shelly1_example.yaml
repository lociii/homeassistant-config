# example

substitutions:
  name: shelly1_example
  friendly_name: Shelly 1 example

<<: !include .shelly1.yaml

light:
  - platform: binary
    id: "${name}_light"
    name: "${friendly_name}"
    output: "${name}_actor"
    restore_mode: RESTORE_DEFAULT_OFF

binary_sensor:
  # device status sensor
  - platform: status
    name: "${name} status"
  # binary sensor for the button press
  - platform: gpio
    name: "${friendly_name} sensor"
    pin:
      number: GPIO5
      inverted: false
    on_press:
      - light.toggle: "${name}_light"
