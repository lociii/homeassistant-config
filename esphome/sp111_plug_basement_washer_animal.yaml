# gosund sp111 v1.1
# 12

<<: !include .sp111.yaml

substitutions:
  name: plug_basement_washer_animal
  friendly_name: Waschmaschine Tier
  cf1_pin: GPIO04
  state_led_pin: GPIO02
  status_led_pin: GPIO00
  actor_pin: GPIO15
  restore_mode: RESTORE_DEFAULT_OFF
  update_throttle: 10s

# connect to basement wifi
wifi:
  ssid: !secret wifi_ssid_basement
  password: !secret wifi_password
  domain: !secret wifi_domain
