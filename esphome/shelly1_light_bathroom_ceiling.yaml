# shelly 1
# 03

<<: !include .shelly1.yaml

substitutions:
  name: light_bathroom_ceiling
  friendly_name: Bad Deckenlicht

light:
  - platform: binary
    id: "${name}"
    name: "${friendly_name}"
    output: "${name}_actor"
    restore_mode: RESTORE_DEFAULT_OFF
